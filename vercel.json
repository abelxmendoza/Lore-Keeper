{
  "buildCommand": "cd apps/web && pnpm install --include-workspace-root && pnpm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install --include-workspace-root",
  "framework": "vite"
}

