# Ignore root monorepo files that might confuse Vercel
# (These paths are relative to apps/web since that's the rootDirectory)
../pnpm-workspace.yaml
../pnpm-lock.yaml
../package.json
../node_modules

# Ignore other workspace apps
../server
../mobile

